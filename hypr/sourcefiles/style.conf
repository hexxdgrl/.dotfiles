general {
    # Gaps between windows
    gaps_in = 5

    # Gaps between window and screen edge
    gaps_out = 7

    # Border width
    border_size = 1

    # Border colors
    col.active_border = rgb(0B017E) #<<active
    col.inactive_border = rgb(313337) #<<inactive

    # No touchy
    resize_on_border = false
    allow_tearing = false

    # Layout
    layout = dwindle

}

# Border decoration

decoration {
    # Corner rounding
    rounding = 2

    # Change transparency of focused and unfocused windows

    active_opacity = 1.0
    inactive_opacity = 1
    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    # Blur

    blur {
        enabled = true
        size = 20
        passes = 3

        vibrancy = 0.2
    }
}

# Layout

dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}
